---
// Institutional identity stamp shown on EVERY hero (home slider + all sub-page
// heroes) so the parent institution is named consistently site-wide. Absolutely
// pinned top-left under the nav, in the .wrap gutter, so it lines up with the
// logo and never collides with the bottom-anchored hero title. Non-interactive.
// The full institutional name as one elegant sentence (wraps gracefully; the
// commas are the conventional Department, University, Country affiliation form).
---
<div class="hero-ident" aria-hidden="true">
  <div class="wrap">
    <p class="hero-ident__line">Center for Astronomy, Space Science and Astrophysics, Independent University, Bangladesh (IUB)</p>
  </div>
</div>
