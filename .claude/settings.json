{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command // \"\"'); if printf '%s' \"$cmd\" | grep -qE '(^|[^[:alnum:]_-])git[[:space:]]+push'; then printf '%s' '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"ask\",\"permissionDecisionReason\":\"git push deploys to production cassa.bd via GitHub Actions. Approve ONLY if you intend to deploy (the PUSH rule).\"}}'; fi",
            "statusMessage": "Checking for git push (deploy gate)..."
          }
        ]
      }
    ]
  }
}
